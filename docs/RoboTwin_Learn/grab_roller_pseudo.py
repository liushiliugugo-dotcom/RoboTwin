# ==========================================
# /RoboTwin/envs/grab_roller.py
# 抓取滚筒任务 (伪代码) 
# 继承自 Base_Task (可以使用基座里的所有超能力)
# ==========================================
class 抓取滚筒任务(Base_Task):

    def 初始化_演示(self, 参数):
        # 召唤老父亲(Base_Task)的能力，把天、地、桌子、相机全建好
        调用父类的_init_task_env_(参数)

    def 加载_演员_和_道具(self):
        # 1. 随机选一个滚筒模型 (粗的或细的)
        滚筒模型 = 随机选择(型号0, 型号2)
        
        # 2. 在桌子上的特定范围内，随机撒下这个滚筒
        初始姿态 = 随机生成位置(x范围, y范围, 随机旋转)
        self.滚筒 = 在物理世界创建实体(模型=滚筒模型, 位置=初始姿态)

    def 机器人表演_play_once(self):
        # ==========================================
        # 【坑了 Piper 的犯罪现场】：强行要求两只手！
        # ==========================================
        左手标签 = "left"
        右手标签 = "right"

        # 第一步：同时去抓！
        self.执行动作(
            计算抓取姿态(目标=滚筒, 用哪只手=左手, 抓滚筒的左边_接触点0),
            计算抓取姿态(目标=滚筒, 用哪只手=右手, 抓滚筒的右边_接触点1)
        )

        # 第二步：同时往上提！
        self.执行动作(
            计算相对位移(用哪只手=左手, 向上提拉 z=0.85),
            计算相对位移(用哪只手=右手, 向上提拉 z=0.85)
        )
        
        返回 场景信息记录

    def 检查是否成功_check_success(self):
        # 胜利条件必须同时满足三点：
        条件1 = 左手夹爪是闭合的？
        条件2 = 右手夹爪是闭合的？
        条件3 = 滚筒的 Z 轴高度 > 0.8米？ (被成功举起来了)
        
        返回 (条件1 AND 条件2 AND 条件3)